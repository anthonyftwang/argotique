Example post list header:

```jsx
<PostListHeader
  titleText="Sortable argots"
  showSort
  showAdd
  sortChangeHandler={() => alert('Sort method changed!')}
  addPostHandler={() => alert('Opening post dialog...')}
/>
```
